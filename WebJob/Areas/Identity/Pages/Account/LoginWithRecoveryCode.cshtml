@page
@model LoginWithRecoveryCodeModel
@{
    Layout = "/Areas/Identity/Pages/Account/_LayoutLogin.cshtml";
    ViewData["Title"] = "Xác minh mã khôi phục";
}

<div class="row flex-center min-vh-100 py-6">
    <div class="col-sm-10 col-md-8 col-lg-6 col-xl-5 col-xxl-4">

        <div class="card">
            <div class="card-body p-4 p-sm-5">
                <div class="row flex-between-center mb-2">
                    <div class="col-auto">
                        <h5>@ViewData["Title"]</h5>
                    </div>
                </div> 
                <p>
                    Bạn đã yêu cầu đăng nhập bằng mã khôi phục. Thông tin đăng nhập này sẽ không được ghi nhớ cho đến khi bạn cung cấp
                    mã ứng dụng xác thực khi đăng nhập hoặc tắt 2FA và đăng nhập lại.
                </p>
                <div class="row">
                    <div class="col-md-12">
                        <form method="post">
                            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                            <div class="form-floating mb-3">
                                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="RecoveryCode" />
                                <label asp-for="Input.RecoveryCode" class="form-label"></label>
                                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                            </div>
                            <button type="submit" class="w-100 btn btn-lg btn-primary">Đăng nhập</button>
                        </form>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}