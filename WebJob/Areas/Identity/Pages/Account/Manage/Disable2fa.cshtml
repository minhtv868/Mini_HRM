@page
@model Disable2faModel
@{
    ViewData["Title"] = "Tắt xác thực hai yếu tố (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h5>@ViewData["Title"]</h5>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Thao tác này chỉ tắt xác thực hai yếu tố 2FA.</strong>
    </p>
    <p>
        Tắt xác thực hai yếu tố (2FA) không làm thay đổi các khóa đã dùng cho ứng dụng xác thực. Nếu bạn muốn thay đổi khóa
        được sử dụng trong ứng dụng xác thực, bạn nên <a asp-page="./ResetAuthenticator">đặt lại các khóa xác thực của mình.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Tắt xác thực hai yếu tố (2FA)</button>
    </form>
</div>
